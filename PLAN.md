# 内容工作站 MVP 开发计划

> 更新时间：2026-08-11  
> 当前阶段：本地优先版本已完成，开始为代码共享与多用户演进做准备  
> 状态说明：`[x] 已完成`、`[~] 进行中`、`[ ] 待开始`

## 1. 项目目标

把“选题输入 → Codex 内容任务 → 稿件与封面管理 → 发布记录 → 数据复盘”整理成一个本地可运行的网页工作台，减少每次做内容时重复描述背景、寻找文件和手工统计数据的成本。

本期面向单人账号使用，优先保证流程清楚、数据可保存、随时能导出，不在 MVP 中接入账号登录、云数据库、平台发布 API 或 Codex 自动调用。

## 2. MVP 验收标准

- 能查看内容总览、近期内容和制作阶段分布。
- 能填写结构化选题信息，并生成可直接复制给 Codex 的标准任务指令。
- 能把选题保存到内容库，按关键词和状态筛选，并查看内容详情。
- 能记录四个平台的播放、互动、涨粉数据，并查看基础复盘结果。
- 能保存账号定位、表达风格和默认平台。
- 数据刷新页面后仍保留，并支持 JSON 导入、导出与清空内容数据。
- 桌面端和移动端均可使用，生产构建通过。

## 3. 可执行任务

| 编号 | 状态 | 任务 | 交付物 / 验收方式 |
|---|---|---|---|
| T01 | [x] | 初始化独立站点项目 | 项目可在 `localhost:3000` 启动 |
| T02 | [x] | 固化产品范围与开发计划 | 本文件包含范围、任务、验收标准和非目标 |
| T03 | [x] | 搭建应用框架与视觉系统 | 侧边导航、顶部操作区、响应式页面骨架 |
| T04 | [x] | 开发工作台总览 | KPI、制作流程、近期内容、下一步提醒 |
| T05 | [x] | 开发新建内容流程 | 结构化表单、输出选择、封面规格、任务指令生成与复制 |
| T06 | [x] | 开发内容库与详情 | 搜索、状态筛选、内容卡片、稿件/封面/发布数据详情 |
| T07 | [x] | 开发素材案例库 | 预置个人实战案例，支持新增案例并在选题中复用 |
| T08 | [x] | 开发数据复盘 | 平台指标录入、聚合指标、表现对比和复盘提示 |
| T09 | [x] | 开发账号设置与本地数据管理 | 账号定位、风格、默认平台、JSON 导入导出、清空内容与案例 |
| T10 | [x] | 接入浏览器本地持久化 | 所有用户输入写入 `localStorage`，刷新后可恢复 |
| T11 | [x] | 完成响应式与可用性检查 | 适配窄屏；表单、按钮、状态提示语义清楚 |
| T12 | [x] | 生产构建验证与修复 | `npm run build` 与 `npm run lint` 均通过 |
| T13 | [x] | 回写最终任务状态并交付 | 本计划状态与实际实现一致，说明已完成项与下一阶段建议 |
| T14 | [x] | 增加选题推荐与同类内容调研 | 支持 B站/小红书直达搜索、视频号搜一搜提示、本地角度推荐和 Codex 联网调研指令 |
| T15 | [x] | 修复内容卡片状态同步 | 右下角直接显示内容项目实时状态，播放数据不再被误当成发布状态 |
| T16 | [x] | 修复议题输入可读性并补充下一步引导 | 输入框在默认与聚焦状态均保持高对比，并展示四步选题调研流程 |
| T17 | [x] | 增加呈现方式选择 | 新建内容支持出镜口播、HTML录屏、混合录制，混合录制标记为推荐 |
| T18 | [x] | 按呈现方式动态配置交付物 | 切换录制方式时自动匹配提词稿、HTML页面、画面映射或切换时间轴 |
| T19 | [x] | 让 Codex 指令适配录制方式 | 任务指令包含呈现方式及对应的口播、页面、镜头和切换要求 |
| T20 | [x] | 内容详情增加制作方案 | 新增制作标签页，展示录制方式、制作结构与四步制作清单 |
| T21 | [x] | 数据复盘支持录制方式比较 | 汇总三种录制方式的内容数量、播放、收藏率和关注转化 |
| T22 | [x] | 统一应用品牌名称 | 应用正式命名为“产品人内容工作站”，同步网页页签、侧栏品牌、项目名称和备份文件名 |
| T23 | [x] | 品牌升级为通用内容平台 | 正式名称调整为“内容工作站”，移除产品经理人群限定，统一网页页签、侧栏、项目与备份名称 |
| T24 | [x] | 三个选题角度改为模型动态生成 | 由服务端调用 OpenAI Responses API，结合议题、账号资料和案例返回三个结构化角度；支持加载、错误与密钥缺失提示 |
| T25 | [x] | 动态选题兼容 DeepSeek | 支持 DeepSeek 与 OpenAI 双提供商；默认优先 DeepSeek V4 Flash，并通过服务端环境变量切换模型 |
| T26 | [x] | 配置并验证 DeepSeek 实时生成 | 密钥保存在被 Git 忽略的 `.env.local`，本地服务已重启，并使用无个人资料的通用议题成功返回三条动态角度 |
| T27 | [x] | 将一键生产拆成阶段式工作流 | 新建内容只生成内容草稿，不再同时请求 HTML 与封面 |
| T28 | [x] | 增加内容稿查看、编辑与人工确认 | 内容详情支持粘贴和编辑 Markdown；未确认时锁定视觉制作 |
| T29 | [x] | 增加 HTML 独立风格与生成任务 | 支持专业科技、极简信息图、杂志卡片、白板讲解，并生成仅含 HTML 的 Codex 指令 |
| T30 | [x] | 增加封面独立风格与生成任务 | 支持高对比科技、大字观点、杂志编辑、人物留白，并独立生成三尺寸封面指令 |
| T31 | [x] | 增加分阶段状态与变更失效规则 | 内容修改后自动回到编辑中，已生成的 HTML 与封面标记为待重新生成 |
| T32 | [x] | 串联新建内容的完整制作流程 | 生成内容任务时自动建立当前项目；本页直接完成内容回填、编辑确认、风格选择与后续任务 |
| T33 | [x] | 修复深色详情面板完成状态可读性 | 内容稿与视觉制作中的次级按钮和完成状态保持足够文字对比度 |
| T34 | [x] | 工作台改为真实项目状态驱动 | 优先事项、完成节点、进度条和下一步清单从最新未完成内容动态计算 |
| T35 | [x] | 增加内容与案例删除能力 | 内容详情和案例卡片提供二次确认删除；同步清理选中态与当前制作状态 |
| T36 | [x] | 清除全部预置测试数据 | 默认内容库、案例库和运营数据为空；旧版本已知演示记录加载时自动清除 |
| T37 | [x] | 增加全量内容数据清空入口 | 设置页可清空内容、案例和运营数据，同时保留账号设置与模型配置 |
| T38 | [x] | 增加历史内容归档入口 | 不经过工作站创建的旧内容可直接录入内容库，并标记内容来源 |
| T39 | [x] | 增加历史资料与数据维护 | 支持发布日期、发布链接、原稿、备注和四平台表现数据维护 |
| T40 | [x] | 清理代码仓库中的个人默认资料 | 新安装使用通用账号占位内容；个人数据和密钥不进入 Git |
| T41 | [x] | 补充本地共享与在线开放说明 | README 明确本地克隆、数据隔离、备份迁移和未来云端演进边界 |
| T42 | [ ] | 建立用户账号与云端数据隔离 | 在线开放前增加登录、用户归属、权限校验和数据迁移机制 |

## 4. 核心数据对象

- 内容项目：标题、目标人群、核心问题、主要观点、素材来源、亲身案例、平台、输出物、制作状态、创建时间。
- 历史内容：内容来源、发布日期、发布链接、原稿、归档备注和平台表现；可与工作站创作内容统一检索和复盘。
- 分阶段制作：内容稿状态、人工确认状态、HTML 状态与风格、封面状态与风格；内容变更会使下游视觉资产失效。
- Codex 任务：账号背景、选题背景、表达要求、稿件结构、平台适配、三尺寸封面要求、交付清单。
- 案例素材：案例名称、行业、背景、采取动作、结果、可支撑观点。
- 发布数据：平台、发布日期、播放量、点赞、收藏、评论、转发、新增关注。
- 账号设置：个人经历、账号定位、目标人群、表达风格、商业目标、默认平台。

## 5. 本期非目标

- 不直接调用 Codex / ChatGPT API；本期生成标准任务指令并一键复制。
- 不自动发布到 B 站、小红书、视频号或抖音。
- 不自动抓取平台数据；本期手工录入，后续可接表格导入或平台接口。
- 不做多用户、权限、云同步和付费系统。
- 不公开部署；当前内容包含个人定位和未发布素材，先按本地工具交付。

## 6. 下一阶段候选

1. 接入 Codex 自动生成与版本回写，让三个独立阶段都能在页面内执行并保留版本。
2. 增加平台发布包导出，包括标题、简介、话题、评论区引导和封面文件。
3. 支持 CSV / Excel 批量导入运营数据，自动计算完播率、互动率和关注转化率。
4. 根据历史数据生成选题建议，并沉淀高表现开头、结构和封面模板。

## 7. 本次验证记录

- `npm run lint`：通过。
- `npm run build`：通过。
- 本地预览地址：`http://localhost:3000/`。
- 当前数据模式：浏览器 `localStorage`，并支持 JSON 导入与导出。
- 旧数据兼容：已有内容自动补充“未设置”录制方式，不覆盖原稿件、状态和运营数据。

## 8. 选题检索实现决策

- B站：使用平台公开关键词搜索页面，保留用户在浏览器中的正常使用方式。
- 小红书：使用关键词搜索页面；部分内容需要用户登录后查看。
- 视频号：没有稳定的公开网页关键词检索入口，本期复制关键词并提示到微信“搜一搜”中选择视频号。
- Codex：生成一份带账号背景、样本字段、真实性约束和差异化判断要求的联网调研指令。
- 本地推荐：根据账号定位提供“误区纠偏、实战方法、转型视角”三个切入角度；明确标注它不是平台热度结论。
- 暂不直接抓取平台页面或要求模型 API Key，避免登录、验证码、反爬和接口变化影响核心内容流程。
