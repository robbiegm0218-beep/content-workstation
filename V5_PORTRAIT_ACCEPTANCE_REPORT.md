# V5 / R18 竖屏独立构图验收报告

日期：2026-08-14

## 结论

R18 已通过。内容工作站现在可以在生成场景方案前选择 `16:9` 或 `9:16`；`9:16` 使用独立竖屏 Composition、纵向信息结构和底部字幕安全区，不是对横版画面的裁切。

## 实现范围

- 页面与持久化：内容记录新增 `videoAspectRatio`，旧数据默认迁移为 `16:9`；切换画幅会使旧场景方案和成片失效。
- Codex 方案：任务按所选画幅明确要求 1920×1080 或 1080×1920；竖屏强调短标题、纵向流程、上下对比和安全区。
- 预览与渲染：Remotion Player 和本地渲染器按画幅选择 `ContentWorkstationLandscape` 或 `ContentWorkstationPortrait`。
- 产物验收：场景 Schema、语义校验、manifest、ffprobe 和 PNG 尺寸检查均支持两种合法宽高组合，并拒绝宽高错配。

## 真实样片

- 来源：沿用 V4 已验收 HTML 演化场景内容，将画幅合同切换为独立 9:16 构图，不新增或虚构素材。
- 分辨率：1080×1920。
- 帧率与时长：30fps，900 帧，30.058667 秒。
- MP4：5,346,071 字节，SHA-256 `2d9e751ddd32d3ef287d33be9c671ca215d13b471f702aa29fad3878e1a57571`。
- Poster：455,466 字节，SHA-256 `92bf93c47a4926091956797b829cd864c8703108378a695251ef99bc5d99e7d2`。
- 本地目录：`outputs/v5-r18-portrait/`（不进入 Git）。

## 画面检查

- 标题、正文、核心判断卡片、纵向流程与进度条均位于竖屏安全区内。
- 字幕使用独立竖屏字号、左右边距和底部位置，不遮挡进度条。
- 流程场景按纵向卡片或双列短卡片排列；对比场景采用上下堆叠；截图场景采用上文案、下素材区域。
- 精确落在场景切换边界的单帧可能处于淡入淡出状态；连续播放正常，poster 继续选择场景内部帧。

## 验证结果

- `npm run build`：通过。
- `npm run video:check`：10 项通过，含 9:16 合同测试与 TypeScript 检查。
- 完整回归：48/48 通过；隐私扫描、ESLint 与构建均通过。首次并发回归中的 SSE 完成事件断言出现一次时序抖动，单测与完整套件复跑均通过。
- 真实渲染 manifest、ffprobe、poster 尺寸和哈希：通过。

## 后续范围

- R19：5～8 分钟长视频的分段渲染、资源限制、取消与恢复。
- R20：安装检查、Doctor 与 Remotion 商业许可证提示。
