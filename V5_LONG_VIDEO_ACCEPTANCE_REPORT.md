# V5 / R19 长视频分段渲染验收报告

> 验收日期：2026-08-14  
> 结论：R19 通过，可进入 R20 安装、Doctor 与许可证提示。

## 1. 验收范围

本次验证的是 5～8 分钟视频的工程能力，包括统一时长合同、场景边界分段、进度反馈、主动中断、检查点续渲、最终合并和产物验收。验收方案由既有 RAG 样例场景扩展而成，不用于评价长视频内容质量。

## 2. 实现结果

- 工作站支持 30 秒（900 帧）、5 分钟（9000 帧）和 8 分钟（14400 帧），统一使用 30fps。
- 长视频最多 120 个场景，单场景最长 900 帧；编辑器持续保证场景连续且总帧数与合同一致。
- 渲染器沿场景边界按约 1800 帧分段，长任务并发限制为 2，超时上限为 2 小时。
- 检查点包含方案与渲染配置哈希、分段帧范围和文件大小；只有三者一致且文件有效时才复用。
- Bridge 和界面可显示当前分段、总分段、合并与校验状态。失败、取消、超时和应用重启中断后，可复用同一 run ID 与工作区继续渲染。
- 渲染进度按整数百分比输出，避免长视频逐帧事件持续写入状态文件。

## 3. 真实中断与恢复

验收输入为 30 个连续场景、9000 帧、1920×1080。分段结果为：

1. 0–1800 帧
2. 1800–3600 帧
3. 3600–5400 帧
4. 5400–7200 帧
5. 7200–9000 帧

第 1 段完成并写入检查点后，主动发送中断信号。重新执行同一工作区时，渲染器报告复用第 1/5 段，从第 2 段继续，最终完成剩余四段和无重编码合并。

## 4. 最终产物

- 视频：`work/v5-r19-long-smoke/output/video.mp4`
- Poster：`work/v5-r19-long-smoke/output/video-poster.png`
- 检查点：`work/v5-r19-long-smoke/output/render-checkpoint.json`
- 分段目录：`work/v5-r19-long-smoke/output/segments/`
- 视频规格：1920×1080、30fps、300.266667 秒
- MP4 大小：30,227,148 字节
- MP4 SHA-256：`19d239771ed875d2e0d546fc53c2b478b75216b3106d8210b91faecb33db37c1`
- 五个分段均记录在检查点中；manifest 路径边界、文件大小、哈希、时长、分辨率、poster PNG 头和尺寸验证通过。

## 5. 边界与下一步

本次没有加入真实 5 分钟旁白、字幕和逐镜素材，因此证明的是稳定渲染与恢复能力，而不是可直接发布的成片质量。R20 需要补齐 Remotion、Chrome、FFmpeg 的环境检测、普通用户安装引导和许可证边界提示。
