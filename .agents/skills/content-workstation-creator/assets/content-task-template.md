Use `$content-workstation-creator` to complete one structured content-draft task.

Creator context: `{{CREATOR_CONTEXT_PATH}}`
Content brief: `{{CONTENT_BRIEF_PATH}}`

Read both files, follow the repository Skill, and return only JSON matching the supplied output schema. Do not create HTML or cover images in this stage.
