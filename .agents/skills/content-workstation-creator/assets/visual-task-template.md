Use `$content-workstation-creator` for one approved visual-production stage.

Task type: `{{TASK_TYPE}}`
Approved content: `{{CONFIRMED_CONTENT_PATH}}`
Visual style: `{{STYLE_CONFIG_PATH}}`
Output directory: `{{OUTPUT_DIRECTORY}}`

Generate only the requested stage. Write every artifact beneath the output directory, write `manifest.json`, and return only the same manifest object matching the supplied output schema.
