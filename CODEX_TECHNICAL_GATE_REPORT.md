# Codex 本地直连技术闸门报告

> 日期：2026-08-11
> 范围：个人本地版 C01～C07
> 结论：**Go，可以进入 Local Bridge 正式开发**

## 验证环境

- macOS
- Node.js 22.22.1
- `codex-cli 0.146.0-alpha.9.2`
- Codex 使用本机 ChatGPT 登录
- `image_generation` 功能状态为 stable
- 本机 Google Chrome 与 `sips` 可用于精确尺寸本地渲染

这里记录的是最低**已验证** Codex 版本，不代表更低版本一定不可用。其他环境后续由 Doctor 做能力检测，不能只比较版本号。

## 验证结论

| 能力 | 结果 | 实际证据 |
|---|---|---|
| JSONL 与 thread ID | 通过 | Runner 解析成功、失败及 `thread.started`；真实内容任务返回 thread ID |
| 仓库 Skill | 通过 | 临时 Git 仓库返回专属标记 `CW-SKILL-1.0` |
| 结构化内容 | 通过 | JSON Schema 返回标题、稿件、时间轴、素材、待补问题和四平台发布包 |
| 单文件 HTML | 通过 | 隔离目录落盘，自包含、无外部 URL、哈希一致，可由 Chrome 打开 |
| 三尺寸封面 | 通过 | 三张独立 PNG 的签名、1600×900 / 1200×900 / 900×1200 实际尺寸和哈希通过 |
| 图片能力 | 通过 | imagegen 生成无字背景，本地排版准确呈现中文标题 |
| 取消 | 通过 | 真实 Codex 进程组收到取消后退出，PID 不再存活 |
| 超时 | 通过 | 自动测试确认超时后先 SIGTERM，必要时 SIGKILL，状态区分为 timeout |
| 产物路径安全 | 通过 | 只接受 `output/` 内普通文件，拒绝路径穿越与符号链接 |

## 关键技术选择

### 内容、HTML、封面分阶段

内容稿先生成、编辑并确认。HTML 和封面只能读取已确认快照，不能一次调用同时生成全部结果。这能避免内容修改后反复浪费图片额度，也使每个阶段都可重试和接受。

### 封面采用混合模式

默认流程为：

1. imagegen 只生成不含标题文字的视觉素材；
2. Codex 根据所选风格创建三个独立比例的本地布局；
3. 本地渲染器叠加准确中文标题并输出精确 PNG；
4. manifest 记录尺寸、哈希和生成模式。

这样既保留图片模型的视觉表现力，又避免中文标题变形、尺寸不准确和机械裁切。

### 保留模板降级

本机验证不需要强制降级，但正式实现必须保留 `codex-template-render`：当图片功能不可用、额度不足或无法返回本地素材时，使用 CSS/SVG 图形和本地渲染继续生成三尺寸封面，并在 manifest 和页面中如实标明生成模式。

### 取消整个进程组

Runner 不只终止最外层 Node 任务，而是终止 Codex 进程组，避免 Codex 启动的渲染器或辅助命令留在后台。取消和超时使用不同状态，便于页面正确提示和重试。

## 已知边界

- 当前只验证个人本机环境，尚未开发 Bridge 服务、Doctor、持久化任务记录和页面自动回填。
- imagegen 会消耗 Codex 使用额度，生成时间明显长于纯文本和 HTML。
- Chrome、图片能力和本地字体在其他电脑上可能不同，必须由 Doctor 实测能力并选择生成模式。
- 真实生成物保存在被忽略的 `work/` 中，不进入 Git；它们是技术验证产物，不是最终个人账号内容。

## 下一阶段入口

进入 A2（C08～C13），开发仅监听 `127.0.0.1` 的 Local Bridge、环境 Doctor、隔离工作区、任务管理、持久化记录和安全边界。A2 完成前不接入现有页面按钮。
