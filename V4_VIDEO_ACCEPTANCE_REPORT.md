# Remotion V4 真实内容双路线验收

> 验收日期：2026-08-14  
> 真实选题：用户问一句，RAG 跑几步？拆开一次对话从提问到生成答案的完整链路  
> 输出规格：16:9、1920×1080、30fps、900 帧、约 30.06 秒

## 1. 验收结论

V4 / R17 已完成。同一个真实选题已经分别完成“直接根据内容生成视频”和“基于已验收 HTML 演化视频”的场景生成、真实 Remotion 渲染、媒体校验、poster 检查和多帧抽查。

- 直接内容路线：适合不依赖 HTML、需要自由重组观点和绑定外部素材的内容；本次需要较多人工调整，素材与知识节点的语义匹配仍需人工负责。
- HTML 演化路线：更适合已经有结构化录屏页的方法论、流程和知识拆解；章节顺序、标题语义和视觉主题更稳定，本次未修改场景文案即可完成成片。
- 两条路线均未配置旁白音频，只使用自动字幕。因此当前成片可用于画面样片、口播辅助或后续配音，不应直接视为最终发布版。
- V4 证明了两条短视频路线均可稳定产出，但没有证明自动视频比 30 秒 HTML 人工录屏更省总耗时。当前价值主要是可重复渲染、自动字幕与减少手工剪辑，而不是更快出片。

## 2. 对比结果

| 指标 | 直接根据内容生成 | 基于 HTML 演化 |
| --- | --- | --- |
| 场景方案 | 12 个场景 | 10 个场景 |
| 内容来源 | 已确认内容稿 | 已确认内容稿 + 已验收 HTML |
| HTML 追溯 | 空 | 记录 HTML run ID、SHA-256 和 11 个章节标题 |
| Codex 场景生成耗时 | 94.30 秒 | 119.43 秒，期间发生一次自动网络重连 |
| 最终有效渲染耗时 | 147.72 秒 | 104.38 秒 |
| MP4 | 6,610,011 字节 | 5,524,826 字节 |
| 人工调整 | 2 组：开场表达；截图场景、时长与视频素材绑定 | 0 次内容调整；1 次工程调整，修复 poster 恰好取在场景淡入边界 |
| 视觉一致性 | 中等，深色科技风统一，但外部视频素材需要人工判断语义 | 较高，沿用 HTML 的米白、黑、红主题和章节顺序 |
| 成片可用性 | 有条件可用；适合作为口播辅助，当前素材镜头需发布前复核 | 有条件可用；适合流程型知识讲解，仍需补旁白或真人口播 |
| 相对 HTML 录屏节省时间 | 未证明；自动渲染约 2.46 分钟，高于 30 秒播放时长 | 未证明；自动渲染约 1.74 分钟，高于 30 秒播放时长 |

## 3. 直接内容路线验收

- 场景方案 run：`run-03009eb8-5dba-465a-abba-b39d27d47660`
- 渲染 run：`run-cea5c7e8-f47c-4e5b-a995-d56e40f8f78f`
- 场景顺序以内容稿为主，不依赖 HTML。
- 为“转成可检索表示”场景绑定了一条本地视频素材，并调整为 screenshot 场景。
- 修改一个场景时长后，其余场景由工作站自动重新平衡，总时间轴仍为 900 帧。
- MP4 为 H.264、1920×1080、30fps、30.058667 秒；自动媒体校验通过。
- 抽帧结果显示标题、正文、字幕和进度条均可读；外部视频素材出现人物近景，是否符合该知识节点需要发布者最终判断。

## 4. HTML 演化路线验收

- HTML 来源 run：`run-1f690da3-3c83-4ad5-8ce7-0b3e8288e517`
- 场景方案 run：`run-9c91c45f-1aa1-48a7-8d29-244a95cc8dd6`
- 最终渲染 run：`run-7f72f8f5-7806-47df-b5a8-11996e16ac9d`
- 场景方案保留 HTML 的标题、冲突、七节点链路和验收表顺序，并记录 11 个可追溯章节标题。
- 生成 10 个视频场景，未引入虚构素材或外部路径。
- 首次渲染暴露 poster 固定取第 210 帧时恰好落在场景淡入起点的问题；已改为选择目标场景内部中点，并增加自动测试。
- 修正后 poster、标题、卡片、字幕和进度条均正常显示。
- MP4 为 H.264、1920×1080、30fps、30.058667 秒；自动媒体校验通过。

## 5. 路线选择建议

- 已经制作并验收 HTML：优先选择 HTML 演化，结构稳定、视觉一致，人工调整更少。
- 没有 HTML，或需要自由使用真人、产品界面、项目截图和外部视频：选择直接内容路线。
- 观点口播：以直接内容路线作为 B-roll 和重点字幕，不建议让自动画面完全替代真人表达。
- 流程、方法论、节点拆解：优先 HTML 演化路线，HTML 继续保留为独立录屏产物。

## 6. V5 启动判断

V4 的短样片技术闭环通过，可以进入 9:16 独立构图验证；但 5～8 分钟长视频仍不应直接启动。下一阶段应先解决旁白同步、素材语义匹配和竖版信息密度，再决定是否扩展长视频。

